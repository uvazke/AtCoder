#include <vector>
#include<iostream>

using namespace std;

int main(void){
	int N;
	double A,B,C;
	cin >> N >> A >> B >> C;
	double b0 = 1,b=1;
	for(int i=0; i< N-1; i++){
		b0 = b0 * double(n+i)/double(i+1) * (B/100.0);
		b += b0;
	}
}
