#include <iostream>
#include <vector>
#include <numeric>
#include <cstdlib>
#include <cmath>
#include <algorithm>
#include <string>
#include <cstring>
#include <queue>
#define myfor(i,N) for(i=0;i<N;i++)
#define myforFL(i,f,l) for(i=f;i<l;i++)
#define myforInv(i,N) for(i=N;i>=0;i--)
#define myforFLInv(i,f,l) for(i=f;i>l;i--)
#define INF pow(10,14)
using namespace std;
long dp[200000][199999];//i番目までのブロックのなかでK組の隣り合うブロックを同じ色で塗る場合の組み合わせの数．

int main(){
	int N,M;
	int K;
	cin >> N >> M >> K;


	return 0;
}